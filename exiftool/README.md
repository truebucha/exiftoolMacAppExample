The shell script pp.sh pack the exiftool library to a single executable file for the source platform

you also could provide you own output path like first commandline argument

example 

pp.sh /tmp/my_name
